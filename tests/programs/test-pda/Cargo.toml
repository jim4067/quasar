[package]
name = "quasar-test-pda"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[lints]
workspace = true

[dependencies]
quasar-lang = { path = "../../../lang" }
quasar-derive = { path = "../../../derive" }

[features]
default = ["alloc"]
alloc = []
debug = []
